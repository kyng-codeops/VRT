addict
future
numpy
opencv-python
Pillow
pyyaml
requests
scikit-image
scipy
torch==2.6.0+cu124  # Pinned: CUDA 12.4 driver (550.x). Install from https://download.pytorch.org/whl/cu124
torchvision==0.21.0+cu124
timm
einops
av
vsrife  # Install with --no-deps to prevent torch upgrade to incompatible cu130 build
vsrvrt>=1.0.0,<=1.1.3  # Cache eviction patch required; see patches/ and apply_patches.py
# vapoursynth: install via conda (pip has unresolvable dependency conflicts
# between vsstubs 1.2.0 and vapoursynth 74rc1). Use: conda install vapoursynth